namespace FluentData
{
    /// <summary>
    /// Database provider for SQL Azure.
    /// Inherits all functionality from <see cref="SqlServerProvider"/> as SQL Azure is compatible with SQL Server.
    /// </summary>
    public class SqlAzureProvider : SqlServerProvider
    {
    }
}
